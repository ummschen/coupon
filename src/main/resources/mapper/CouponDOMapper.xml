<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.coupon.api.mapper.CouponDOMapper" >
  <resultMap id="BaseResultMap" type="com.coupon.api.entity.CouponDO" >
    <!--
      WARNING - @mbg.generated
    -->
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="coupon" property="coupon" jdbcType="VARCHAR" />
    <result column="encryption" property="encryption" jdbcType="VARCHAR" />
    <result column="salt" property="salt" jdbcType="VARCHAR" />
    <result column="coupon_type" property="couponType" jdbcType="VARCHAR" />
    <result column="price" property="price" jdbcType="DECIMAL" />
    <result column="channel_code" property="channelCode" jdbcType="VARCHAR" />
    <result column="business_code" property="businessCode" jdbcType="VARCHAR" />
    <result column="status" property="status" jdbcType="INTEGER" />
    <result column="end_time" property="endTime" jdbcType="TIMESTAMP" />
    <result column="write_off_account" property="writeOffAccount" jdbcType="VARCHAR" />
    <result column="create_time" property="createTime" jdbcType="TIMESTAMP" />
    <result column="update_time" property="updateTime" jdbcType="TIMESTAMP" />
  </resultMap>


  <sql id="baseColumn">
    `id`, `coupon`,salt,encryption, `coupon_type`,`price`,`channel_code`,`business_code`,`status`,`end_time`,`write_off_account`,`create_time`,`update_time`
  </sql>



  <select id="queryList" resultType="com.coupon.api.dto.CouponDTO" parameterType="com.coupon.api.entity.CouponDO">
    select <include refid="baseColumn"/>  from `coupon`
    <include refid="sqlWhere"/>
    order by `create_time` desc
    <if test="pageIndex != null and pageSize != null">
      limit #{pageIndex}, #{pageSize}
    </if>

  </select>
  <select id="queryCount" parameterType="com.coupon.api.entity.CouponDO" resultType="int">
    SELECT count(*) from `coupon`
    <include refid="sqlWhere"/>
  </select>

  <sql id="sqlWhere">
    <where>
      <if test='id != null '>and id = #{id}</if>
      <if test='coupon != null and coupon != ""'>and coupon = #{coupon}</if>
      <if test='couponType != null and couponType != ""'>and coupon_type = #{couponType}</if>
      <if test='channelCode != null and channelCode != ""'>and channel_code = #{channelCode}</if>
      <if test='businessCode != null and businessCode != ""'>and business_code = #{businessCode}</if>
      <if test='writeOffAccount != null and writeOffAccount != ""'>and write_off_account = #{writeOffAccount}</if>
      <!--<if test='end_time != null and end_time != ""'>and end_time = #{coupon}</if>-->
      <if test='status != null '>and `status` = #{status}</if>
      <if test='price != null and price !=0'>and `price` = #{price}</if>
      <if test='startCreateTime != null and startCreateTime != ""'>and   `create_time` <![CDATA[>=]]>#{startCreateTime,jdbcType=TIMESTAMP} </if>
      <if test='endCreateTime != null and endCreateTime != ""'>and    `create_time`<![CDATA[<=]]>#{endCreateTime,jdbcType=TIMESTAMP}</if>
    </where>
  </sql>



  <insert id="insertBatch" parameterType="java.util.List">
      insert into coupon ( `coupon`,salt,encryption, `coupon_type`,`price`,`business_code`,
                                `status`,`end_time`,`create_time`) values
        <foreach collection="list" item="item" index="index" separator=",">
            (
              #{item.coupon},
              #{item.salt},
              #{item.encryption},
              #{item.couponType},
              #{item.price},
              #{item.businessCode},
              #{item.status},
              #{item.endTime},
              now()

            )
        </foreach>
   </insert>

  <update id="distribute" >
    update  coupon set channel_code=#{coupon.channelCode} ,set status=1
    where coupon_type =#{coupon.couponType}
          and business_code =#{coupon.businessCode}
          and status=0
          and channel_code =''
    order by id
    limit #{num}
  </update>
  
  <select id="selecyByCouponCode" resultType="com.coupon.api.entity.CouponDO">
    select <include refid="baseColumn"/> from coupon where coupon =#{coupon};
  </select>
</mapper>